export async function validateRequiredFields(payload: any): Promise<void> {
  for (const payloadField of Object.keys(payload)) {
    const value = payload[payloadField];
    if (!value) {
      const error = new Error(`${payloadField} is required`) as any;
      error.statusCode = 400;
      throw error;
    }
  }
}
